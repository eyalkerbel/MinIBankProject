import * as API from "../Api";
import chargers from "../reducers/ChargesReducer";

// const apiMiddleware = store => next => action => {
//     if (!action.meta || action.meta.type !== 'api') {
//         return next(action);
//     }

//     API.getChargers.then(chargers => )
// }